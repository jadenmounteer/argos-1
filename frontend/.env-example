VITE_PICOVOICE_ACCESS_KEY= # this is for the wake word. Go to Picovoice Console, create an account, train a model, and download the .ppn into public/models.
VITE_KERNEL_URL= # base URL for the ARGOS-1 Kernel (e.g. http://localhost:8080 for local backend)