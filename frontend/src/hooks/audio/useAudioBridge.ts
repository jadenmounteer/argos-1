/**
 * Placeholder audio bridge hook.
 * Later this will orchestrate playing UI sounds and agent speech.
 */
export function useAudioBridge() {
  return {
    playBeep: () => {},
    playAcknowledge: () => {},
  }
}

